# Ревью последней версии API документации (api-docs.json)

**Дата проверки**: [текущая дата]  
**Статус**: ❌ Найдена критическая проблема в схеме CustomerDetail

---

## ❌ Критическая проблема

### Проблема: Неправильная схема CustomerDetail

**Текущая реализация**:
```json
"CustomerDetail": {
  "required": ["countryId", "countryName", "email", "name"],
  "properties": {
    "id": { ... },
    "name": { ... },
    "email": { ... },
    "phone": { ... },
    "countryId": { ... },        // ❌ Не должно быть
    "countryName": { ... },      // ❌ Не должно быть
    "createdAt": { ... },        // ❌ Не должно быть (readonly)
    "updatedAt": { ... }         // ❌ Не должно быть (readonly)
  },
  "description": "Extended detailed representation with all fields including countryName..."
}
```

**Проблемы**:
1. ❌ Содержит `countryId` — должно быть исключено (системное поле)
2. ❌ Содержит `countryName` — это поле только для `CustomerListItem` (денормализация для таблиц), не для `CustomerDetail`
3. ❌ Содержит `createdAt` и `updatedAt` — readonly поля, должны быть исключены
4. ❌ Неправильный `required` — включает `countryId` и `countryName`
5. ❌ Неправильное описание — говорит "with all fields including countryName", что неверно

**Согласно требованиям** (из API_DESIGN_REQUIREMENTS.md):

**CustomerDetail должен содержать**:
- `id` (UUID)
- `name` (string)
- `email` (string)
- `phone` (string)

**CustomerDetail НЕ должен содержать**:
- ❌ `countryId` (системное поле)
- ❌ `countryName` (это для ListItem, не для Detail)
- ❌ `createdAt` (readonly, системное поле)
- ❌ `updatedAt` (readonly, системное поле)

**Исправление**:
```json
"CustomerDetail": {
  "required": ["email", "id", "name", "phone"],
  "type": "object",
  "description": "Extended representation without readonly fields. Excludes: countryId, createdAt, updatedAt. Used with: application/vnd.api.customer.detail+json",
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid",
      "description": "Customer unique identifier",
      "example": "123e4567-e89b-12d3-a456-426614174000"
    },
    "name": {
      "type": "string",
      "minLength": 2,
      "maxLength": 100,
      "description": "Customer name",
      "example": "John Doe"
    },
    "email": {
      "type": "string",
      "format": "email",
      "maxLength": 255,
      "description": "Customer email address",
      "example": "john.doe@example.com"
    },
    "phone": {
      "type": "string",
      "maxLength": 20,
      "description": "Customer phone number",
      "example": "+1-555-0101"
    }
  }
}
```

**Ключевые изменения**:
- ✅ Убрать `countryId`
- ✅ Убрать `countryName` (это только для ListItem!)
- ✅ Убрать `createdAt`
- ✅ Убрать `updatedAt`
- ✅ Изменить `required` на `["email", "id", "name", "phone"]`
- ✅ Исправить `description` на правильное описание

---

## ✅ Что остальное работает правильно

- ✅ Content Negotiation — все Media Types присутствуют
- ✅ Error handling — все endpoints используют Error schema
- ✅ Пагинация, фильтрация, сортировка — правильно настроены
- ✅ CustomerListItem — правильно содержит countryName
- ✅ CustomerLookup — правильно (id + name)
- ✅ Customer — правильно (базовое представление)
- ✅ Все остальные схемы корректны

---

## 📋 Чеклист исправлений

**Критично**:
- [ ] Исправить схему `CustomerDetail`:
  - [ ] Убрать `countryId` из properties
  - [ ] Убрать `countryName` из properties
  - [ ] Убрать `createdAt` из properties
  - [ ] Убрать `updatedAt` из properties
  - [ ] Изменить `required` на `["email", "id", "name", "phone"]`
  - [ ] Исправить `description` на правильное описание

---

## 💡 Пояснение

**Разница между CustomerDetail и CustomerListItem**:

- **CustomerDetail** (`application/vnd.api.customer.detail+json`):
  - Для детального просмотра одного ресурса
  - Чистые данные без системных полей
  - Используется: карточки профиля, формы редактирования
  - **НЕ содержит**: countryId, countryName, createdAt, updatedAt

- **CustomerListItem** (`application/vnd.api.customer.list+json`):
  - Для списков/таблиц
  - Денормализованные данные для производительности
  - Используется: таблицы со странами без дополнительных запросов
  - **Содержит**: countryName (денормализованное поле для производительности)

**Почему countryName не в CustomerDetail**:
- `CustomerDetail` предназначен для отображения ОДНОГО ресурса в деталях
- Если нужна страна — клиент может сделать дополнительный запрос или использовать base representation
- `countryName` в ListItem нужен для таблиц, где показывается много записей сразу и нужна производительность

---

## Итоговая оценка

**Общий результат**: ~95% готово

**Что отлично**:
- ✅ Все остальные компоненты работают правильно
- ✅ Content Negotiation реализовано
- ✅ Все другие схемы корректны

**Что нужно исправить**:
- ❌ **КРИТИЧНО**: Схема CustomerDetail содержит лишние поля

**После исправления CustomerDetail API будет полностью готово!**

---

Готово для передачи дизайнеру API.

